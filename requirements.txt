# Core dependencies
openai>=1.58.0
jsonschema>=4.23.0
numpy>=1.24.0
faiss-cpu>=1.8.0
unidecode>=1.3.8
transformers
torch
sentence_transformers

# File processing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Optional but recommended
python-dotenv>=1.0.0
tomli>=2.0.0; python_version<"3.11"

# Visualization dependencies
networkx>=3.0
scipy>=1.10.0  # Required by NetworkX for PageRank and other algorithms
python-louvain>=0.16
Jinja2>=3.1.0
minify-html>=0.16.0  # HTML minification for smaller output files

# =============================================================================
# doc-converter — модуль предобработки документов (пакет doc_converter/)
#
# После pip install -r requirements.txt установите сам пакет:
#   pip install -e ./doc_converter
#
# Системные зависимости (установить отдельно):
#   - pandoc  — для конвертации DOCX (https://pandoc.org/installing.html)
#   - marker-pdf при первом запуске PDF скачает ML-модели (~1–2 GB)
#
# VLM (OpenAI/Anthropic) намеренно не включён — используйте --vlm-backend off
# =============================================================================

# CLI и конфигурация
typer>=0.12.0
rich>=13.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# XLSX
openpyxl>=3.1.0
pandas>=2.0.0

# DOCX
pypandoc>=1.13
python-docx>=1.1.0

# PDF
marker-pdf>=0.2.0
pymupdf>=1.24.0

# Изображения: эвристики OpenCV + OCR-fallback для таблиц (без VLM)
opencv-python-headless>=4.8.0
Pillow>=10.0.0
img2table>=1.2.0

# Опционально: метаданные академических PDF через GROBID (нужен сервер)
# grobid-client-python>=0.0.8

# Тесты doc-converter (можно не ставить в prod)
pytest>=8.0.0
pytest-cov>=4.0.0